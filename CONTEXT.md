# Pi 个人扩展

本项目维护彼此独立的 Pi 交互增强能力。本上下文定义面向用户的思考翻译语言，避免把模型公开的摘要误称为完整内部推理。

## Language

**可见思考块**：
Pi 向用户公开显示的 assistant `thinking` 内容，可能是推理摘要，但不等同于模型完整的内部思维过程。
_Avoid_：思维过程、完整思维链、隐藏推理

**思考译文**：
可见思考块对应的中文显示内容；它帮助用户阅读，但不取代或改变原始思考内容。
_Avoid_：中文思维链、译后思考

**忠实中文化**：
在不增删事实的前提下，将原文改写为自然、简洁的中文，并原样保留代码、路径和专有名词。
_Avoid_：摘要、意译扩写、逐字直译

**用户任务**：
从一条用户消息开始处理，到下一条用户消息开始处理前的完整工作过程，包括其中的工具调用和自动重试。
_Avoid_：Agent run、turn、session

**思考译文时间线**：
同一个用户任务中按产生顺序排列的全部思考译文；下一个用户任务开始时重置。
_Avoid_：翻译历史、永久记录

**翻译模型**：
用户显式指定、专门生成思考译文的 Pi 模型；未配置或不可用时不自动改用主 Agent 模型。
_Avoid_：当前模型、备用模型
